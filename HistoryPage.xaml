<Page x:Class="IshTaluy.HistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IshTaluy"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HistoryPage">

    <Grid Background="#F2E9E4">
        <ListView x:Name="lstView1">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=playerName}" Header="Player" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=result}" Header="Result" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=time}" Header="Time" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
